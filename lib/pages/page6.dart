import 'package:flutter/material.dart';

class pagina6 extends StatelessWidget {
  const pagina6({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text('contenido de pestaña 6')
        ],
      ),
    );
  }
}